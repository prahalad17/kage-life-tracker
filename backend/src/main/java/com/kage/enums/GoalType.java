package com.kage.enums;

/**
 * Defines whether an activity is something to do or avoid.
 */
public enum GoalType {
    GENERIC,     // independent goal
    METRIC,      // future: tied to metric
    ACTION       // future: driven by actions

}
